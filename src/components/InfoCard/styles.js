import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    flexDirection: 'row',
    alignItems: 'center',
    marginBottom: 20,
    marginHorizontal: 10,
  },
  icon: {
    width: 42,
    height: 42,
  },
  text: {
    fontSize: 12,
    fontWeight: '600',
  },
});

export default styles;
