import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    marginLeft: 32,
    flex: 1,
  },
  subtitle: {
    fontSize: 20,
    color: '#000',
    marginTop: 40,
    marginBottom: 16,
  },
  row: {
    flexDirection: 'row',
  },
});

export default styles;
