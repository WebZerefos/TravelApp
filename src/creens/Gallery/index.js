import {Text, SafeAreaView} from 'react-native';
import React, {memo} from 'react';

const Gallery = () => {
  return (
    <SafeAreaView>
      <Text>Gallery</Text>
    </SafeAreaView>
  );
};

export default memo(Gallery);
